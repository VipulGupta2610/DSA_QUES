package pkg;

public class validPalindrome {
    public static void main(String[] args) {

    }

    static boolean isPalindrome(String s) {
        s = s.replace(",", "");
        s = s.replace(" ", "");
        s = s.replace(":", "");
        int start = 0;
        int end = s.length();
        while (start<=end) {
            if (s.charAt(end))
        }
    }
}