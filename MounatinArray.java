public class MounatinArray {
    public static void main(String[] args) {
        int[] arr = { 0, 1, 0 };
    }

    static int peakIndexInMountainArray(int[] arr) {
        int idx = helper(arr, 0, arr.length-1);
        return idx;
    }

    static int helper(int[] arr, int start, int end) {
        while (start < end) {
            
        }
    }
}
